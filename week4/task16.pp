#include<iostream>
using namespace std;
main(){
int area;
    string figure;
    cout<<"enter the figure";
    cin>>figure;
    if(figure=="square"){

        int length;
        cout<<"enter length";
        cin>>length;
area=length*length;
cout<<area;

    }
    
    
}